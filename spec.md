# 每日英语听写 Spec

`spec.md` 是“每日英语听写”的当前产品事实主干。它只记录已经明确并由当前工程承载的产品定位、业务边界、核心概念、用户路径和验收标准。

## 1. 产品定位

每日英语听写是一个微信小程序 + CloudBase 项目，用于把英文材料和可选音频组织成可听、可读、可新建、可删除、可管理的学习材料。

## 2. 当前主线

- 用户可以使用本地接口，不依赖 CloudBase。
- 用户可以使用云端接口，数据进入 CloudBase。
- 本地接口和云端接口分离，不自动同步。
- 首页先展示测试入口和云端入口；测试入口进入本地资源、本地新建、本地练习，云端入口进入云端资源、云端新建、云端练习。
- 用户可以新增、重命名和删除普通分类，并把未归类材料中选中的一个或多个材料导入指定分类。
- 公共资源是只读分类，普通用户不能复制、编辑、移动、删除、排序其中的材料。
- 资源页提供全部资源虚拟入口，聚合展示当前可见材料，但不写入本地缓存或云端数据库。
- 资源页提供搜索框和搜索按钮；输入时显示命中列表，点击结果或搜索按钮会进入材料所在分类并高亮材料。
- 未归类材料一直显示在分类列表底部，不能删除，不能重命名。
- 删除普通分类时，该分类内材料自动迁移到未归类材料。
- 新建内容按完整材料保存。
- 新建材料时可以只保存文本，也可以同时从微信会话文件选择音频。
- 导入和编辑采用手动保存；输入时只更新页面临时状态，点击保存才写入数据。
- 材料有音频就播放。
- 材料没有音频就作为阅读材料。
- 材料页是主要操作界面，支持直接播放、暂停、继续、从头播放、倍速播放、单条无限循环、自动下一条、进度展示、拖动跳转、查看原文和管理删除。
- 普通模式点击材料进入原文查看；管理模式通过编辑入口修改标题、正文和音频。
- 听写练习是独立模块，从已有有音频材料中随机播放，不复制材料，不改变资源管理结构。
- 听写练习支持全部随机、分类随机和练习组随机。
- 练习播放默认不显示原文，用户可以在播放页手动查看或隐藏原文。
- 材料页和听写练习页共用当前设备播放设置，支持 `0.75x`、`1x`、`1.25x`、`1.5x` 倍速、单条无限循环和播放结束 3 秒后自动下一条。
- 练习组保存在当前设备小程序缓存中，按本地和云端模式分区；云端练习组当前不进入 CloudBase，不跨设备同步。

## 3. 核心概念

### 3.1 资料库

资料库是材料来源的最高层组织。

当前资料库类型：

- 本地资料库：保存在当前设备的小程序缓存中。
- 云端资料库：保存在 CloudBase 中。
- 系统库/通用听力：后续由管理员维护的云端资料来源。

当前界面中，资料库也承担用户自定义分类职责。用户可以新增和重命名普通分类，把材料移动到其他分类，并在分类内长按拖拽调整材料顺序。

用户可以删除普通自定义分类。删除普通分类时，该分类内材料自动迁移到未归类材料。未归类材料一直显示在分类列表底部，不能删除，不能重命名。

公共资源是只读分类。普通用户只能查看和播放公共资源里的材料，不能复制、编辑、移动、删除或排序。

全部资源是虚拟入口，用于聚合展示当前可见材料。它不属于 `SourceLibrary`，不进入本地缓存、生成文件或云端数据库。

### 3.2 材料

材料是产品的基本学习单位，可以是一句话、一段话或一篇文章。

材料包含：

- 名称：列表和听力入口展示。
- 英文内容：用于原文展示和阅读。
- 音频数量：用于判断是否可以直接播放。
- 排序值：用于分类内长按拖拽排序和稳定展示。

### 3.3 听力音频

听力音频是材料可播放的音频文件。音频来源包括：

- 用户上传音频。
- 云端生成音频。

用户可以在导入材料时从微信会话文件选择音频，也可以在原文详情页替换材料音频。替换音频后，材料使用新的当前音频记录。

上传音频支持：

- `mp3`
- `m4a`
- `wav`

当前不做整篇音频自动对齐，不做音频自动切句。

## 4. 用户路径

### 4.1 本地使用

1. 用户打开小程序。
2. 用户进入测试入口。
3. 用户选择本地资源、本地新建或本地练习。
4. 用户在本地资源中看到本地资料库和材料。
5. 用户在材料页直接播放有音频的材料，或点材料标题查看原文。
6. 用户进入管理模式，可以新建材料、从未归类材料中选择一个或多个材料导入到普通分类、移动或删除自己的材料。
7. 用户可以编辑自己在本地创建的材料，并替换材料音频。

### 4.2 云端使用

1. 用户打开小程序。
2. 用户进入云端入口。
3. 用户选择云端资源、云端新建或云端练习。
4. 小程序通过 CloudBase 读取云端资料库和材料。
5. 用户在材料页直接播放有音频的材料，或点材料标题查看原文。
6. 用户进入管理模式，可以新建材料、从未归类材料中选择一个或多个材料导入到普通分类、移动或删除自己的云端材料。
7. 用户可以编辑自己的云端材料、上传替换音频，或通过已配置的 TTS 生成音频。

## 5. 当前功能范围

当前工程包含：

- 微信小程序 TypeScript 工程。
- Vant Weapp npm 依赖。
- 首页双通道工作区入口。
- 本地资源和本地新建。
- 云端资源和云端新建。
- 本地练习和云端练习。
- 材料保存、读取和删除。
- 材料编辑和音频替换。
- 分类新增、重命名、删除并迁移材料、材料移动分类和分类内长按拖拽排序。
- 全部资源虚拟入口和资源页材料搜索。
- 首页、资源、材料、原文详情、新建材料、导入已有材料页面。
- 本地测试样例 `local-assets/public/dog-wolf-friendship/`。
- 自动测试、类型检查、乱码检查、坏命名检查和页面入口检查。

当前工程不包含：

- 已部署云环境。
- 已创建真实数据库集合。
- 已配置真实腾讯云 TTS 密钥。
- 已内置大批系统材料。
- 微信开发者工具中的 `miniprogram_npm/` 构建产物。

## 6. 不做范围

当前产品不做：

- 自动分句。
- 逐句播放。
- 听写输入和自动评分。
- 音频导出。
- 语法教学。
- 语法分析。
- 口语评分。
- 发音纠错。
- 作文批改。
- AI 老师聊天。
- 社交社区。
- 课程商城。
- 复杂考试题库。
- 多级分类体系。
- 材料市场。
- 自动联网查词。
- 整篇音频自动对齐。
- 音频自动切句。

## 7. 数据边界

当前已确认的数据对象：

- `SourceLibrary`：资料库。
- `Material`：材料。
- `ListeningAudio`：听力音频。
- `DictionaryEntry`：词义记录。

本地数据保存在当前设备或微信开发者工具的小程序缓存中，当前缓存键为 `listen.localData`。云端数据保存在 CloudBase 中。本地和云端不自动同步。

`local-assets/` 是仓库级测试样例目录，不是小程序运行时写入目录。用户在小程序里新建的材料不会自动生成或修改 `local-assets/` 文件。

`local-assets/` 当前固定使用三个顶层分区：

- `public/`：公共资源样例，默认进入公共资源分类，只读。
- `uncategorized/`：未分类材料样例，默认进入未分类材料。
- `user/`：用户资源样例，默认进入用户资源分类。

样例材料目录为 `local-assets/<section>/<folder-id>/`。`<section>-<folder-id>` 组成生成材料 ID，`<folder-id>` 用于派生默认标题。文件夹名使用小写字母、数字和连字符。样例材料文件使用固定通用命名：`text.txt`、`audio.mp3|m4a|wav`。`metadata.json` 只用于样例材料首次初始化分类覆盖，不记录用户运行时移动分类结果。

## 8. 验收标准

- 首页能进入测试入口和云端入口。
- 测试入口能进入本地资源、本地新建、本地练习。
- 云端入口能进入云端资源、云端新建、云端练习。
- 本地资源无需云环境即可显示样例材料。
- 材料页支持查看原文、直接播放、管理导入、编辑和删除。
- 导入已有材料页支持从未归类材料选择一个或多个材料、全选、取消选择和导入已选。
- 材料页支持播放、暂停、继续、从头开始、倍速播放、单条无限循环、自动下一条、进度展示和拖动跳转。
- 听写练习页支持全部随机、分类随机、练习组随机、播放、暂停、继续、从头开始、下一条、查看原文、隐藏原文、倍速播放、单条无限循环、自动下一条、进度展示和拖动跳转。
- 练习组页支持从当前资料库有音频材料中勾选材料并保存练习组，支持删除练习组。
- 资源页支持新增分类、重命名普通分类、删除普通分类并迁移材料；未归类材料一直置底展示且不可删除、不可重命名。
- 资源页支持搜索当前可见材料；输入时显示结果列表，点击结果或搜索按钮进入材料所在分类并高亮材料。
- 全部资源入口聚合展示当前可见材料，不作为真实分类保存。
- 公共资源只读，普通用户不能编辑、移动、删除、排序或复制公共材料。
- 材料管理模式支持移动分类和长按拖拽排序。
- 详情页支持查看模式和编辑模式；查看模式只显示原文，编辑模式支持修改标题、正文和替换音频。
- 新建材料页支持从微信会话文件选择可选音频，并在保存材料后绑定到该材料。
- 新建材料页和详情编辑页只在点击保存后写入标题和正文。
- 没有音频的材料作为阅读材料存在，不阻塞导入和查看。
- 听写练习只抽取有音频且未归档的材料；随机队列一轮内不重复，播完一轮后重新洗牌。
- `npm.cmd run verify` 通过。
- `npm.cmd run build` 能生成微信开发者工具运行所需 JS。

## 9. 开源协议

当前项目选择 MIT License。完整协议文本以根目录 `LICENSE` 文件为准。
