<view class="page">
  <view class="section">
    <view class="title">管理</view>
    <view class="subtitle">仅管理员 openid 可以维护系统库。</view>
  </view>

  <view class="panel">
    <view class="admin-line">openid：{{openid}}</view>
    <view class="{{isAdmin ? 'admin-allowed' : 'admin-denied'}}">{{isAdmin ? '已获得管理权限' : '当前账号没有管理权限'}}</view>
  </view>
</view>
