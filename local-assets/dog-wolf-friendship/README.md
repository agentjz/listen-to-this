# Dog And Wolf Friendship

本目录是“每日英语听写”的本地测试样例，按真实本地文件夹方式组织，不作为 TypeScript seed 数据。

## 内容

- `text.txt`：完整英文材料。
- `audio.wav`：材料音频样例。
- `image.svg`：材料配图样例。

文件夹名 `dog-wolf-friendship` 是材料唯一标识，也会派生默认标题。

## 音频对应关系

- `audio.wav` 对应完整材料。

当前只保留一个音频样例。产品导入时按完整材料导入；有音频就播放，没有音频就作为阅读材料。
